= form_for @prototype, html: { class: "container proto-new"} do |f|
  .col-md-8.col-md-offset-2
    %header.row.user-nav.row
      .col-md-12
        = f.text_field :title, placeholder: "Title", class: "proto-new-title"
    .row
      .col-md-12
        .cover-image-upload
          = f.fields_for @prototype.image[0] do |i|
            = i.file_field :image, id: "file-input"
            = i.hidden_field :user_id, value: current_user.id
      .col-md-12
        %ul.proto-sub-list.list-group
          %li.list-group-item.col-md-4
            .image-upload
              = f.fields_for @prototype.image[1] do |i|
                = i.file_field :image, id: "file-input"
                = i.hidden_field :user_id, value: current_user.id
          %li.list-group-item.col-md-4
            .image-upload
              = f.fields_for @prototype.image[2] do |i|
                = i.file_field :image, id: "file-input"
                = i.hidden_field :user_id, value: current_user.id
          %li.list-group-item.col-md-4
            .image-upload-plus
              %span +
    .row.proto-description
      .col-md-12
        = f.text_field :catch_copy, placeholder: "Catch Copy"
      .col-md-12
        = f.text_area :concept, placeholder: "Concept", cols: "30", rows: "4"
      .col-md-12
        %h4 Tag List
        .row
          .col-md-3
            %input{type: "text", placeholder: "Web Design"}/
          .col-md-3
            %input{type: "text", placeholder: "UI"}/
          .col-md-3
            %input{type: "text", placeholder: "Application"}/
    .row.text-center.proto-btn
      %button.btn.btn-lg.btn-primary.btn-block
        = f.submit "Publish"
